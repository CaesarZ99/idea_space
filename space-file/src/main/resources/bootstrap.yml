server:
  port: 8001
spring:
  application:
    name: space-file # 服务名称
  cloud:
    nacos:
      config:
        file-extension: yml
        refresh-enabled: true
        server-addr: 139.9.240.113:8848
